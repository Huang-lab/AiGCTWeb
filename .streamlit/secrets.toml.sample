# Copy this file to secrets.toml (gitignored) for local development and
# set appropriate key value.
# Set the same key in the Streamlit Community Cloud app's Secrets UI for deploy.
OPENROUTER_API_KEY = "..." 
