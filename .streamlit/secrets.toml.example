# Copy this file to secrets.toml (gitignored) for local development, and set
# the same key in the Streamlit Community Cloud app's Secrets UI for deploy.
# Get a free key at https://console.groq.com -> API Keys.
GROQ_API_KEY = "gsk_..."
